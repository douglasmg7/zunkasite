section {
	background-color: var(--white-lighter);
	padding: 1em;
	margin: 2em 1em;
}
.title {
    margin-top: 0;
}
.motoboy-delivery {
    border: 1px solid var(--black-lighter);
    padding: 1em;
    border-radius: 4px;
    margin-bottom: 1em;
}
