Change 3088 port to 443 on production.

Test forgotten password.

Verify app.log size rotation.

Reduce stock after order placed.

Some models not update update updatedAt.

Price in R$2.412,09 into db to search.

use canceledAt to filter orders.

Remover senhas do servidor

carrinho atualizar quando alterar o preço dos produtos.

Não remover usuário quando ele pagar a conta, manter informações por questões de segurança.

Num of products to not commercialize must be in to a config, not harded coded

npm packages
rimraf
webpack-dev-server

https://www.npmjs.com/package/nodemailer-stub-transport

Avoid user see order from other user.

Centralize all email configurations.
  - transporter

User removed, must remove all info after two months.

Insert label for input
  signin