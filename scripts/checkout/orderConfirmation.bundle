import _search from '../lib/_search';

// Vue.
var app = new Vue({
  el: '#app',
  data: {
    order: order
  },
  filters: {
    formatMoney: function(val){
      return 'R$ ' + val.replace('.', ',').replace(/\B(?=(\d{3})+(?!\d))/g, ".");
    }
  }  
});

