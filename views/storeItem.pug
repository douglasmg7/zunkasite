doctype html
html(lang="en")
  head
    meta(charset='utf-8')
    meta(name="viewport" content="width=device-width, initial-scale=1")
    title Equipamentos de informatica.
    include components/semantic.css.pug
  body
    h1 '#{product.dealer}'
    #app
    //- Make pug data disponible to vue instance.
    script.
      //- let vueProduct = '#{JSON.stringify(product)}'
      let vueProduct = '#{stringify(product)}'
      //- window.renderData = {}
      //- window.renderData.initSearch = '#{initSearch}'
      //- window.renderData.username = '#{username}'
      //- window.renderData.group = '#{group}'   
      //- console.log(`locals: ${product}`);   
    include components/jquery.js.pug
    include components/semantic.js.pug
    include components/vue.js.pug
    include components/vue-resource.js.pug
    script(src='/bundleStoreItem.js')
