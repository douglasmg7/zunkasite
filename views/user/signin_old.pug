extends ../layout/layout_vue

block title
  title Zunka - Autenticação

block css
  link(rel="stylesheet" href="/css/user/signin.css" charset="utf-8")

block content 
  .panel
    h2.title Identificação              
    p.success-message(v-show='this.successMessage !== ""') {{successMessage}}
    label(for='email') E-mail
    input(type='text' id='email' v-model='email')
    label(for='password') Senha
    input(type='password' id='password' v-model='password')
    label 
      a.find-password(href='/user/forgottenPassword') Esqueceu a senha?
    input.btn-primary(type='submit' value='Entrar' @click='signin()')
    p.warn-message(v-show='this.warnMessage !== ""') {{warnMessage}}
    hr
    label Não tem cadastro?
    input.btn-secondary(type='submit' value='Cadastrar' @click='signup()')

append internal_scripts
  script.
    let warnMessage = !{JSON.stringify(warnMessage)};
    let successMessage = !{JSON.stringify(successMessage)};

append external_scripts
  script(src='/js/user/signin.js')        