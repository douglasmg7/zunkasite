doctype html
//- html(lang="en" style="overflow: hidden; box-sizing: border-box")
html(lang="en")
    head
        meta(charset='utf-8')
        meta(name="viewport" content="width=device-width, initial-scale=1")
        meta(name="description" content="Loja especializada na venda de laptop")
        block meta
        block title
        block css
            link(rel="stylesheet" href="/css/layout/normalize.css" charset="utf-8")
            link(rel="stylesheet" href="/css/layout/reset.css" charset="utf-8")
            link(rel="stylesheet" href="/css/layout/layout.css" charset="utf-8")
            link(rel="stylesheet" href="/fontawesome/css/all.min.css" charset="utf-8")
    block internal_style

    body
        #header
            include header
        #menu-container
            #menu
                include menu
        main#app
            block content
        block footer
            include footer

    block internal_scripts
        script.
            // Search for products.
            function _search(text){
                window.location.href = `/all?page=1&search=${text}`;
            }
            // Csrf token.
            let csrfToken = !{JSON.stringify(csrfToken)};

    block external_scripts
        script(src='/js/product/layout.js?004')
